# Intermediate HTML from md2pdf.py (regenerable)
*.html
!index.html
!bento.html

# Live sheet snapshot fetched via curl (regenerable, see refresh_guides.py)
_live_data.json

# Tool / IDE state
.claude/
.vscode/
.idea/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# D1 import/snapshot files contain mentor + country tokens — keep local only
import.sql
snapshot_*.sql
CountryTokens.csv
MentorTokens.csv
country_tokens.csv
mentor_tokens.csv

# Pages / Workers deploy staging — rebuilt before each deploy
public/

# Wrangler local state (auth, dev D1, build cache)
.wrangler/
